export global ProfileState {
    in-out property <image> cover-image;
    in-out property <string> title;
    in-out property <string> year;
    in-out property <string> path_to_profile;
    in-out property <string> temp_path;
}
